# Redrob Candidate Ranking Engine (Track 1)

## What this does
Given a natural language ICP query represented via targeted heuristic weights, this system:
- Identifies high-fit target technical profiles from an unstandardized stream of 100,000 candidate records.
- Enriches records with deterministic timeline verification to neutralize synthetic profile traps.
- Processes live platform engagement criteria across a 23-parameter behavioral multiplier matrix.
- Yields a highly optimized, tie-broken monotonic top-100 cohort accompanied by zero-hallucination, fact-grounded structural evaluations.

## Why this is different
Unlike traditional text sorting pipelines or unverified vector lookups:
- **Timeline Honeypot Neutralizers:** Programmatically maps and isolates logical timeline contradictions (e.g., skill experience outdating employer founding markers).
- **Service vs. Product Modifiers:** Evaluates company architectures explicitly to favor true product engineering histories over uniform service consultation environments.
- **Pure Python Performance Scaling:** Implements process-chunking mapping to maximize multi-core execution boundaries completely offline, finishing in fractions of the sandbox time allocation.

## System Capabilities
- **Deterministic Edge-Case Sorting:** Guaranteed 101-row strict output formatting with absolute structural tiebreakers utilizing alphabetical candidate indexing.
- **Cross-Platform Safety:** Engineered with pure path abstracts (`pathlib`) and structural write open variables (`newline=""`) to safeguard platform translation across Windows and Linux kernel execution environments.

## Local Setup & Execution
1. Create and activate isolated environment:
   ```bash
   python -m venv venv
   source venv/Scripts/activate
